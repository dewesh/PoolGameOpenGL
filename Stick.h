#ifndef STICK_H
#define STICK_H

#include "Game.h"

 class Stick
 {
 	point power;
 public:
 	void setPosition(point);
 	void setPower(point);
 	void render();
 	void update();
 	/* data */
 };
#endif
 